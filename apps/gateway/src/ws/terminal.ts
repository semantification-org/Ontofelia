import { FastifyInstance } from 'fastify';
import type { GatewayContext } from '../context.js';

export default async function wsTerminalRoutes(_fastify: FastifyInstance, _ctx: GatewayContext) {
  // Terminal WebSocket routes — reserved for future implementation
}