{
  "name": "@ontofelia/semantic-memory",
  "version": "0.0.1",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "node -e \"const fs = require('fs'); const path = require('path'); fs.mkdirSync('dist/ontologies', { recursive: true }); fs.readdirSync('src/ontologies').filter(f => f.endsWith('.ttl')).forEach(f => fs.copyFileSync(path.join('src/ontologies', f), path.join('dist/ontologies', f)))\"",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@ontofelia/core": "workspace:*",
    "@ontofelia/reasoner": "link:../reasoner",
    "oxigraph": "^0.5.8"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vitest": "^2.0.5"
  },
  "license": "Apache-2.0"
}
